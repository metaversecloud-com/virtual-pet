apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Workload template for the `vpet0` service. Reusable across envs; the dev overlay
# adds the KEDA scaler + interceptor and drops the direct Ingress.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/name: vpet0
    includeSelectors: false
